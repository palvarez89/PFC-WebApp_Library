/*global define: false, navigatorPG: false, require: false, jQuery: false */
define(["scripts/jquery-1.7.2.min.js"], function () {
	// Raw jQuery does not return anything, so return it explicitly here.
	return jQuery;
});